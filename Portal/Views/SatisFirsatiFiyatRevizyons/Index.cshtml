@model IEnumerable<Portal.Models.SatisFirsatiFiyatRevizyon>

@{
    ViewBag.Title = "Index";
}



<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<div class="table-scrollable" style="margin-top:15px">
    <table class="table table-striped table-bordered table-hover">
        <thead class="flip-content">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Fiyat)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Tarih)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SatisFirsati.Note)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Fiyat)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Tarih)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SatisFirsati.Note)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Kaydet", new { id = item.Id, satisFirsatId = item.RefSatisFirsatiId }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                    </td>
                </tr>
            }
        </tbody>
</table>

</div>

