@model IEnumerable<Portal.Models.SiteKontrol>
@{
    ViewBag.Title = "List";
}


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="table-scrollable">
    <table class="table table-striped table-bordered table-hover">
        <thead class="flip-content">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.SiteKontrolAdi)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SiteKontrolAciklama)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SiteKontrolSira)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.SiteKontrolAdi)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SiteKontrolAciklama)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SiteKontrolSira)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.SiteKontrolID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.SiteKontrolID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.SiteKontrolID })
                    </td>
                </tr>
            }
        </tbody>
</table>
</div>

